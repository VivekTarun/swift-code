// In this function we have internal parameter and external parameter
// we refer external parameter with internal parameter in the function

func myFunc(_ eman: Int) {
    //name -> external parameter
    // eman -> internal parameter
    print(eman)
}


//changing the value with underscore to work with internal and external paramater.
myfunc(value)